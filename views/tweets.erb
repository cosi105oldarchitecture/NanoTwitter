
<ol>
  <% (1..@total).each do |num| %>
    <li>
      "<%= @redis.hget("#{@user.handle}:#{num}", 'body') %>"
      <br/>
      - <%= @redis.hget("#{@user.handle}:#{num}", 'author_handle') %>
    </li>
  <% end %>
</ol>
